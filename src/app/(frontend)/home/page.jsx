export default async function home() {
    // const res = await fetch(process.env.NEXT_PUBLIC_BASE_URL + "/api/movies");
    // const moivies = await res.json();
    // console.log(moivies);
  return (
    <div>
        <p>Home Page</p>
    </div>
  );
}